Note:

files here are links from/to ../Tools/simulation/gazebo-classic/sitl_gazebo-classic/models/r1_rover   - so that Git can pick them up

use ./copy_to_Tools.sh to prepare the fresh tree for SITL compilation.

use ./relink_from_Tools.sh  to restore link when changing the file in the original folder.


